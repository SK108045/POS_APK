{
  "name": "oraforge-pos-app",
  "version": "1.0.0",
  "description": "Oraforge POS Android wrapper via Capacitor",
  "private": true,
  "scripts": {
    "build": "echo 'No frontend build step — using hosted server'",
    "cap": "cap"
  },
  "dependencies": {
    "@capacitor/android": "6.2.0",
    "@capacitor/core": "6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.0"
  }
}
